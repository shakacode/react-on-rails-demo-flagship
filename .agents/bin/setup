#!/usr/bin/env bash
# Generated by shakacode/agent-workflows agent-workflow-seam-doctor --init.
# Install dependencies and prepare the database without starting a long-lived dev server.
set -euo pipefail
cd "$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
exec env ALLOW_DEMO_RENDERER_PASSWORD=true bin/setup --skip-server "$@"
